# Bville custom ordering (SolidJS)

Pizza builder, cart, delivery/pickup — branded for Bville Pizza & Grill.

```bash
cd order-app
npm install
npm run build
```

Build output goes to `../public/order/` and is served at `/order/` on the PHP site.
